package ui

import kotlinx.css.Color
import kotlinx.css.whiteAlpha

/**
 * Colors used in website.
 */

val blueBackgroundColor = Color.darkBlue.withAlpha(0.8)

val blueNavigationColor = Color.blue.withAlpha(0.8)

val darkBackgroundColor = Color.darkGrey.withAlpha(0.1).withAlpha(0.5)

val lighterDarkBackgroundColor = Color.darkGrey.withAlpha(0.1)

val greyTextColor = whiteAlpha(0.8)